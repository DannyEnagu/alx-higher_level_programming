#!/usr/bin/python3


def number_keys(a_dictionary):
    """Returns numbers of keys in a dictionary
       Args:
        @a_dictionary: dictionary input

       Retrn:
        Total number of dict keys
    """
    return len(list(a_dictionary))
